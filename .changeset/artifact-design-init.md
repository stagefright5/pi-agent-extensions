---
"@stagefright5/pi-artifact-design": minor
---

Add the artifact-design skill: design fundamentals, a Vite dev server with HMR and an
Artifact-runtime harness (theme tri-state, host-ground simulation, CSP headers,
`window.artifactAgent` stub), a single-file build via vite-plugin-singlefile, a
publish validator, and the glossary-sidebar pattern.
