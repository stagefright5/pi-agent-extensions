---
"@stagefright5/pi-artifact-design": minor
---

Add the artifact-design skill with design fundamentals, a Vite dev server with HMR, and an Artifact-runtime harness. The harness includes a three-state theme control, host-ground simulation, CSP headers, and a `window.artifactAgent` stub. Also add a single-file build via vite-plugin-singlefile, a publish validator, and the glossary-sidebar pattern.
