# Changesets

Run `vp run changeset` for every user-visible package change. Select each affected package and its semantic version bump.

To release, run `vp run version-packages` and commit the generated versions and changelogs. Then run `vp run release`.
