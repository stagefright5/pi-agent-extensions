# Jenkins CLI Operations

Pi extension wrapper for the `jenkins-cli-operations` Agent Skill copied from Claude Desktop.

The extension exposes the bundled `SKILL.md` through pi's `resources_discover` hook, so it appears as the `/skill:jenkins-cli-operations` command after `/reload` or restart.

The skill includes helper scripts under `scripts/` and references under `references/`.
