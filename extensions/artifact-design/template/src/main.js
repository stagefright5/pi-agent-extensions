import "./style.css";

// Vite replaces this module in place on save; the page keeps its scroll and state.
// Anything you attach here ships inlined in the built single file.
if (import.meta.hot) {
  import.meta.hot.accept();
}
